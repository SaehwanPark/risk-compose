# Installation

Python 3.11 or newer is required.

Install the bundled package:

```bash
pip install risk-compose
```

With `uv`:

```bash
uv pip install risk-compose
```

The package includes curated runtime CSV tables, CLI workflows, and TUI/GUI
frontends needed for supported model versions. No CMS or AHRQ source archive
checkout is required for normal use.
